{
  "name": "engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "plan": "node dist/cli.js --out out",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
